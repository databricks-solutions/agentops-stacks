# Setup guide — {{ .input_project_name }}

End-to-end configuration to get this project deploying. The flow:

1. [Prerequisites](#1-prerequisites) — what your account/workspace need to have first
2. [Unity Catalog setup](#2-unity-catalog-setup) — catalogs, grants, run-as identities
3. [Local development](#3-local-development) — Databricks CLI profiles for dev work
4. [Fill in `databricks.yml`](#4-fill-in-databricksyml) — workspace hosts and run-as
5. [CI/CD credentials](#5-cicd-credentials) — secrets and variables for the chosen platform
6. [First deploy](#6-first-deploy) — validate and deploy to dev

---

## 1. Prerequisites

- Three Databricks workspaces (dev, staging, prod). They can be separate workspaces or environments within one workspace, as long as you can scope catalogs and identities per environment.
- A Unity Catalog metastore attached to each workspace, with permission to create catalogs (or someone who can create them for you).
- {{ if (eq .input_cloud `azure`) -}}
An Azure account with permission to create app registrations (for the staging and prod service principals).
{{- else if (eq .input_cloud `aws`) -}}
A Databricks account or workspace admin who can create service principals for staging and prod.
{{- else if (eq .input_cloud `gcp`) -}}
A GCP project where you can create service accounts for staging and prod, or a Databricks workspace admin who can create Databricks service principals.
{{- end }}
- {{ if (eq .input_cicd_platform `github_actions`) -}}
A GitHub repository to push this project to.
{{- else if (eq .input_cicd_platform `github_actions_for_github_enterprise_servers`) -}}
A GitHub Enterprise Server repository to push this project to.
{{- else if (eq .input_cicd_platform `gitlab`) -}}
A GitLab project to push this project to.
{{- else if (eq .input_cicd_platform `azure_devops`) -}}
An Azure DevOps project and repository.
{{- end }}

---

## 2. Unity Catalog setup

### 2a. Create catalogs

This project expects one catalog per environment:

| Environment | Default catalog name |
|---|---|
| dev | `{{ .input_project_name }}_dev` |
| staging | `{{ .input_project_name }}_staging` |
| prod | `{{ .input_project_name }}_prod` |

If your org has different catalog naming conventions, override the `catalog` value under each target in `databricks.yml`.

In each workspace, as a metastore or catalog admin:

```sql
CREATE CATALOG IF NOT EXISTS {{ .input_project_name }}_dev;
CREATE CATALOG IF NOT EXISTS {{ .input_project_name }}_staging;
CREATE CATALOG IF NOT EXISTS {{ .input_project_name }}_prod;
```

### 2b. Grant permissions to the deploying identity

The user (for dev) or service principal (for staging/prod) that runs `databricks bundle deploy` needs these grants on the relevant catalog:

```sql
GRANT USE CATALOG ON CATALOG {{ .input_project_name }}_<env> TO `<identity>`;
GRANT CREATE SCHEMA ON CATALOG {{ .input_project_name }}_<env> TO `<identity>`;
```

The bundle creates the schema (`{{ .input_project_name }}`) inside the catalog on first deploy, so the deploying identity also needs to be able to create volumes inside that schema:

```sql
GRANT CREATE VOLUME ON SCHEMA {{ .input_project_name }}_<env>.{{ .input_project_name }} TO `<identity>`;
```

`WRITE VOLUME` on the `artifacts` volume is granted automatically to the volume's creator. If you delegate volume creation, grant `WRITE VOLUME` explicitly to whoever runs MLflow.

### 2c. Create service principals (staging + prod)

Production-mode targets in `databricks.yml` use `run_as` to define who the bundle's resources execute as. The default is the deploying user (you), which is fine for getting started but not for real production.

{{ if (eq .input_cloud `azure`) -}}
For Azure, create one SP per environment that needs an automated deployer (staging and prod):

**Option A — Azure CLI** (fastest):

```bash
az ad sp create-for-rbac --name {{ .input_project_name }}-staging-sp --skip-assignment
az ad sp create-for-rbac --name {{ .input_project_name }}-prod-sp --skip-assignment
```

Each command prints `appId`, `password`, and `tenant` — save all three for each SP. You'll use them for `run_as` and CI/CD secrets. `--skip-assignment` avoids granting Azure subscription roles; the SP only needs Databricks workspace access (granted below).

**Option B — Azure portal**: Azure Active Directory → App registrations → New registration. For each app, generate a client secret under Certificates & secrets. Save the Application (client) ID, Directory (tenant) ID, and secret value.

After creating each SP, update `databricks.yml` to reference it in `run_as`:

```yaml
targets:
  staging:
    run_as:
      service_principal_name: <staging-app-id>
  prod:
    run_as:
      service_principal_name: <prod-app-id>
```
{{ else if (eq .input_cloud `aws`) -}}
For AWS:

1. In the Databricks account console → Service principals → Add. Create one SP for staging and one for prod.
2. Generate an OAuth secret (client ID + client secret) for each SP.
3. Add each SP to the appropriate workspace and grant the Unity Catalog permissions from step 2b.
4. Update `databricks.yml` to reference the SP in `run_as`:

   ```yaml
   targets:
     staging:
       run_as:
         service_principal_name: <staging-sp-application-id>
     prod:
       run_as:
         service_principal_name: <prod-sp-application-id>
   ```
{{ else if (eq .input_cloud `gcp`) -}}
For GCP:

1. In the Databricks account console → Service principals → Add. Create one SP for staging and one for prod (Databricks-managed SP, not a GCP service account directly).
2. Generate an OAuth secret for each.
3. Grant Unity Catalog permissions per step 2b.
4. Update `databricks.yml` to reference the SP in `run_as`:

   ```yaml
   targets:
     staging:
       run_as:
         service_principal_name: <staging-sp-application-id>
     prod:
       run_as:
         service_principal_name: <prod-sp-application-id>
   ```
{{- end }}

### 2d. Grant each SP workspace and `/Shared/` access

After the SPs exist, they need to be wired into the corresponding workspaces. In **each workspace** (staging in the staging workspace, prod in the prod workspace), as a workspace admin:

1. **Add the SP as a workspace service principal.** Admin Settings → Identity and access → Service principals → Add. Use the Application ID (from step 2c) as the SP's identifier. Grant **Workspace access: Can use**.
2. **Grant `/Shared/` write access.** The bundle creates an MLflow experiment under `/Shared/`. The SP needs `Can Manage` on `/Shared/` to create the experiment. Workspace browser → `/Shared/` → ⋮ menu → Permissions → Add → select the SP → `Can Manage`.
3. **Apply the Unity Catalog grants from step 2b**, substituting the SP's Application ID for `<identity>`. Use backticks around the Application ID in SQL:

   ```sql
   GRANT USE CATALOG ON CATALOG {{ .input_project_name }}_staging TO `<staging-app-id>`;
   GRANT CREATE SCHEMA ON CATALOG {{ .input_project_name }}_staging TO `<staging-app-id>`;
   ```

Repeat for prod against the prod catalog and prod SP.

---
{{ if (eq .input_use_lakebase `yes`) }}
## 2e. Lakebase (Autoscaling Postgres) for agent memory

This project uses a Lakebase Autoscaling project for agent memory. The bundle
declares it in `resources/lakebase.yml` (`postgres_projects.memory`) and
`databricks bundle deploy` creates the project, its `production` branch, and a
read-write compute endpoint (0.5–2 CU; scale-to-zero after 5 min idle on dev,
always-on in staging/prod).

This project is configured for **{{ .input_memory_type }}** memory:
{{ if (or (eq .input_memory_type "short_term") (eq .input_memory_type "both")) }}
- **Short-term memory** — a LangGraph Postgres checkpointer
  (`databricks_langchain.AsyncCheckpointSaver`) that preserves conversation history
  within a session. Pass `session_id` in the request's `custom_inputs` (or a
  `conversation_id` context) to resume a thread.
{{ end }}{{ if (or (eq .input_memory_type "long_term") (eq .input_memory_type "both")) }}
- **Long-term memory** — a per-user semantic store
  (`databricks_langchain.AsyncDatabricksStore`) surfaced to the agent through the
  `get_user_memory` / `save_user_memory` / `delete_user_memory` tools. Pass
  `user_id` in the request's `custom_inputs` to scope memories per user; without it
  the tools no-op. Semantic search uses the embedding endpoint set by
  `DATABRICKS_EMBEDDING_ENDPOINT` (default `databricks-gte-large-en`, 1024 dims).
{{ end }}
Prerequisites and notes:

- The deploying identity needs **CAN MANAGE on Lakebase projects** in the workspace.
- The app connects with a short-lived, Lakebase-scoped OAuth credential minted by
  the `databricks-langchain` memory SDK — no static password. The app resource in
  `databricks.yml` grants the app service principal `CAN_CONNECT_AND_CREATE`.
{{ if (or (eq .input_memory_type "long_term") (eq .input_memory_type "both")) }}- Long-term memory queries the embedding endpoint, so the app service principal
  needs **CAN QUERY** on `databricks-gte-large-en` (or whatever
  `DATABRICKS_EMBEDDING_ENDPOINT` points to).
{{ end }}- **Grant the app service principal `CREATE` on schema `public` (one-time, required).**
  On a fresh Lakebase project, schema `public` is owned by `pg_database_owner`. The
  bundle's `CAN_CONNECT_AND_CREATE` app-resource grant lets the app SP *connect* and
  gives it `USAGE`, but **not** `CREATE ON SCHEMA public` — so the memory SDK's
  first-run `CREATE TABLE ... migrations` fails with `permission denied for schema
  public`, and the agent silently runs *without* memory (it still answers; the memory
  tools just no-op). This is the most common reason memory "doesn't work" after a
  clean deploy. Fix it once, connected as the database owner (the deploying user):

  ```sql
  GRANT USAGE, CREATE ON SCHEMA public TO "<app-service-principal-client-id>";
  ```

  Find the app SP client id with `databricks apps get {{ (regexp "_").ReplaceAllString .input_project_name "-" }}-{{ (regexp "_").ReplaceAllString .input_initial_agent_name "-" }}`
  (field `service_principal_client_id`). The app retries setup on every request, so
  memory starts working on the next invocation — no redeploy or restart needed.
- **Deploy the app before connecting locally.** If you connect from your laptop
  first, *your* user owns the memory schema and the deployed app then hits
  `permission denied`. Deploy first so the app SP owns its tables. See the
  `lakebase-ops` skill to recover if this happens.
- `LAKEBASE_ENDPOINT` in the agent's `app.yaml` comes prefilled with the derivable
  path to the auto-created read-write endpoint
  (`projects/{{ (regexp "_").ReplaceAllString .input_project_name "-" }}-memory/branches/production/endpoints/primary`).
  You normally don't need to touch it. If you renamed the branch or created a custom
  endpoint, verify the exact path with:

  ```bash
  databricks postgres list-endpoints projects/{{ (regexp "_").ReplaceAllString .input_project_name "-" }}-memory/branches/production
  ```

---
{{ end }}
## 3. Local development

Configure a Databricks CLI profile for your dev workspace so you can run `databricks bundle validate` and `databricks bundle deploy` from your machine.

```bash
databricks auth login --host https://<your-dev-workspace>.{{ if (eq .input_cloud `azure`) -}}azuredatabricks.net{{- else if (eq .input_cloud `aws`) -}}cloud.databricks.com{{- else -}}gcp.databricks.com{{- end }} --profile dev
```

This stores credentials in `~/.databrickscfg`. Verify with `databricks current-user me --profile dev`.

Optional: profiles for staging and prod can be configured the same way if you need to inspect or troubleshoot those workspaces locally. Day-to-day deploys to staging and prod go through CI/CD, not your machine.

Sync dependencies:

```bash
uv sync
```

This generates `uv.lock` (the resolved dependency tree). **Commit `uv.lock` to git** — CI workflows cache against it, and committing it gives deterministic builds across local and CI environments.

`databricks-connect` is a dev dependency — pin it in `pyproject.toml` to match your cluster's runtime version if you'll be running Spark code locally against a remote cluster.

---

## 4. Fill in `databricks.yml`

Open `databricks.yml` and replace the TODO placeholders:

```yaml
targets:
  dev:
    workspace:
      host: https://<your-dev-workspace>.{{ if (eq .input_cloud `azure`) -}}azuredatabricks.net{{- else if (eq .input_cloud `aws`) -}}cloud.databricks.com{{- else -}}gcp.databricks.com{{- end }}
  staging:
    workspace:
      host: https://<your-staging-workspace>.{{ if (eq .input_cloud `azure`) -}}azuredatabricks.net{{- else if (eq .input_cloud `aws`) -}}cloud.databricks.com{{- else -}}gcp.databricks.com{{- end }}
  prod:
    workspace:
      host: https://<your-prod-workspace>.{{ if (eq .input_cloud `azure`) -}}azuredatabricks.net{{- else if (eq .input_cloud `aws`) -}}cloud.databricks.com{{- else -}}gcp.databricks.com{{- end }}
```

If you set up service principals in step 2c, update `run_as` for staging and prod:

```yaml
targets:
  staging:
    run_as:
      service_principal_name: <staging-sp-application-id>
  prod:
    run_as:
      service_principal_name: <prod-sp-application-id>
```

---

## 5. CI/CD credentials

{{ if (or (eq .input_cicd_platform `github_actions`) (eq .input_cicd_platform `github_actions_for_github_enterprise_servers`)) -}}
### GitHub Actions

Add the following secrets to the repository. Either via the UI — **Settings → Secrets and variables → Actions** — or via the GitHub CLI:

{{ if (eq .input_cloud `azure`) -}}
| Secret | Value |
|---|---|
| `STAGING_AZURE_SP_TENANT_ID` | Tenant ID of the staging SP |
| `STAGING_AZURE_SP_APPLICATION_ID` | Application (client) ID of the staging SP |
| `STAGING_AZURE_SP_CLIENT_SECRET` | Client secret for the staging SP |
| `PROD_AZURE_SP_TENANT_ID` | Tenant ID of the prod SP |
| `PROD_AZURE_SP_APPLICATION_ID` | Application ID of the prod SP |
| `PROD_AZURE_SP_CLIENT_SECRET` | Client secret for the prod SP |

Via `gh` CLI (replace `<org>/<repo>` with your repo):

```bash
gh secret set STAGING_AZURE_SP_TENANT_ID --repo <org>/<repo> --body "<tenant>"
gh secret set STAGING_AZURE_SP_APPLICATION_ID --repo <org>/<repo> --body "<appId>"
gh secret set STAGING_AZURE_SP_CLIENT_SECRET --repo <org>/<repo> --body "<password>"
gh secret set PROD_AZURE_SP_TENANT_ID --repo <org>/<repo> --body "<tenant>"
gh secret set PROD_AZURE_SP_APPLICATION_ID --repo <org>/<repo> --body "<appId>"
gh secret set PROD_AZURE_SP_CLIENT_SECRET --repo <org>/<repo> --body "<password>"
```
{{ else -}}
| Secret | Value |
|---|---|
| `STAGING_WORKSPACE_TOKEN` | Personal access token (or M2M OAuth token) for the staging workspace's service principal |
| `PROD_WORKSPACE_TOKEN` | Personal access token (or M2M OAuth token) for the prod workspace's service principal |

Via `gh` CLI:

```bash
gh secret set STAGING_WORKSPACE_TOKEN --repo <org>/<repo> --body "<token>"
gh secret set PROD_WORKSPACE_TOKEN --repo <org>/<repo> --body "<token>"
```
{{- end }}

Under **Settings → Actions → General → Workflow permissions**, enable "Read and write permissions" so jobs can comment on PRs if you extend the workflows to do so.
{{ else if (eq .input_cicd_platform `gitlab`) -}}
### GitLab

In the project, go to **Settings → CI/CD → Variables** and add the following as **protected** variables (so they're only available to protected branches/tags):

{{ if (eq .input_cloud `azure`) -}}
| Variable | Value |
|---|---|
| `STAGING_AZURE_SP_TENANT_ID` | Directory (tenant) ID of the staging app registration |
| `STAGING_AZURE_SP_APPLICATION_ID` | Application (client) ID of the staging app registration |
| `STAGING_AZURE_SP_CLIENT_SECRET` | Client secret value for the staging app (mark masked) |
| `PROD_AZURE_SP_TENANT_ID` | Tenant ID for the prod app registration |
| `PROD_AZURE_SP_APPLICATION_ID` | Application ID for the prod app registration |
| `PROD_AZURE_SP_CLIENT_SECRET` | Client secret value for the prod app (mark masked) |
{{ else -}}
| Variable | Value |
|---|---|
| `STAGING_WORKSPACE_TOKEN` | PAT or OAuth token for the staging service principal (mark masked) |
| `PROD_WORKSPACE_TOKEN` | PAT or OAuth token for the prod service principal (mark masked) |
{{- end }}

In **Settings → CI/CD → General pipelines → CI/CD configuration file**, set the path to:

```
.gitlab/pipelines/{{ .input_project_name }}-triggers-cicd.yml
```

Under **Deployments → Environments**, create `staging` and `production` environments so pipelines can be approval-gated and the variables can scope to each environment.
{{ else if (eq .input_cicd_platform `azure_devops`) -}}
### Azure DevOps

1. Create a **variable group** named `{{ .input_root_dir }}` under **Pipelines → Library**. Add the following variables:

   {{ if (eq .input_cloud `azure`) -}}
   | Variable | Value | Mark secret |
   |---|---|---|
   | `STAGING_AZURE_SP_TENANT_ID` | Directory (tenant) ID of the staging app registration | no |
   | `STAGING_AZURE_SP_APPLICATION_ID` | Application (client) ID of the staging app registration | no |
   | `STAGING_AZURE_SP_CLIENT_SECRET` | Client secret value for the staging app | yes |
   | `PROD_AZURE_SP_TENANT_ID` | Tenant ID for the prod app registration | no |
   | `PROD_AZURE_SP_APPLICATION_ID` | Application ID for the prod app registration | no |
   | `PROD_AZURE_SP_CLIENT_SECRET` | Client secret value for the prod app | yes |
   {{ else -}}
   | Variable | Value | Mark secret |
   |---|---|---|
   | `STAGING_WORKSPACE_TOKEN` | PAT or OAuth token for the staging service principal | yes |
   | `PROD_WORKSPACE_TOKEN` | PAT or OAuth token for the prod service principal | yes |
   {{- end }}

2. Create a **new pipeline** under **Pipelines → Pipelines → New pipeline → Azure Repos Git** → select this repo → **Existing Azure Pipelines YAML file** → pick `.azure/devops-pipelines/{{ .input_project_name }}-bundle-cicd.yml`.

3. Under the pipeline's settings, link it to the variable group from step 1.

4. Set a **branch policy** on `main` requiring the `BundleCI` stage to pass before merging PRs (Branches → main → Branch policies → Build validation).
{{- end }}

---

## 6. First deploy

From your machine, with the dev profile from step 3:

```bash
databricks bundle validate -t dev --profile dev
databricks bundle deploy -t dev --profile dev
```

The first deploy creates the schema and the `artifacts` volume in `{{ .input_project_name }}_dev`. The MLflow experiment is created under `/Shared/{{ .input_project_name }}/dev` with the volume as artifact location.

After this, pushes to `main` deploy to staging via CI/CD, and tags matching `v*` deploy to prod.

---

## Troubleshooting

**`token refresh: Refresh token is invalid`** — your CLI profile's OAuth token has expired. Re-run `databricks auth login --profile <name>`.

**`schema does not exist` or `volume does not exist`** — the bundle creates these on deploy. If you destroyed the bundle (`bundle destroy`) and now deploy fails, run `bundle deploy` again; it will recreate them.

**`CREATE SCHEMA permission denied`** — the deploying identity needs `CREATE SCHEMA` on the catalog. See section 2b.

**`run_as.service_principal_name not found`** — the SP must exist in the workspace as a service principal user (not just in the IdP). See section 2c.

**Production mode rejected my deploy** — `mode: production` enforces that the bundle doesn't use user-scoped paths and that `run_as` is set. If you haven't set up service principals yet, change `run_as` to use your username temporarily, or deploy to dev only.

**`Parent directory does not exist: /Shared/...`** — the MLflow experiment is being placed under a workspace folder that doesn't exist. The template uses a flat path (`/Shared/<bundle.name>_<bundle.target>`) to avoid this — if you customized the experiment `name` in `resources/experiment.yml` to a nested path, either create the parent folder in the workspace or revert to a single-level path.

**`The experiment was created with a bad artifact location`** — the `artifact_location` in `resources/experiment.yml` must have an explicit URI scheme (`dbfs:/Volumes/...`), not just `/Volumes/...`. `bundle validate` accepts either; only deploy enforces the scheme.

**CI error: `Unable to verify checksums signature: openpgp: key expired` during Terraform download** — your pinned Databricks CLI in CI is too old for `bundle: engine: direct` and is falling back to the Terraform engine. Bump `cli_version` in `library/template_variables.tmpl` to v0.295.0 or later.
{{ if (eq .input_use_lakebase `yes`) }}
**Agent memory silently doesn't work — app logs show `permission denied for schema public` / `CREATE TABLE ... checkpoint_migrations` / `store_migrations`** — the app service principal can connect to Lakebase but lacks `CREATE ON SCHEMA public`, so the memory SDK can't create its tables and the agent degrades to running without memory. Grant it once as the database owner — see the grant step in section 2e.

**`This endpoint databricks-... is deprecated` (400) on every agent invocation** — the model set in `LLM_ENDPOINT` (in `src/agents/<agent>/app.yaml`) has been retired on your workspace. Pick a current one from `databricks serving-endpoints list` and update `LLM_ENDPOINT`, then redeploy the app.

**`project slug already exists in the workspace` when deploying the Lakebase memory project** — Lakebase Postgres project slugs (here `{{ (regexp "_").ReplaceAllString .input_project_name "-" }}-memory`) are unique per workspace and reserved through a **7-day soft-delete retention window**. If you previously deployed then deleted the project (e.g. `bundle destroy`), the slug stays reserved until its `purge_time`, and a redeploy fails even though the project no longer appears in `databricks postgres list-projects`. Inspect the soft-deleted project and force an early purge to free the slug:

```bash
# Confirm it's soft-deleted (shows delete_time and purge_time):
databricks api get "/api/2.0/postgres/projects/{{ (regexp "_").ReplaceAllString .input_project_name "-" }}-memory"

# Purge now instead of waiting for purge_time:
databricks api delete "/api/2.0/postgres/projects/{{ (regexp "_").ReplaceAllString .input_project_name "-" }}-memory?purge=true"
```

Then redeploy. Alternatively, wait until `purge_time` and the slug frees itself.
{{ end }}
