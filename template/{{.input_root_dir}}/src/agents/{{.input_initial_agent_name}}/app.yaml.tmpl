command: ["uv", "run", "python", "app/start_server.py"]

env:
  - name: CATALOG
    value: "${var.catalog}"
  - name: SCHEMA
    value: "${var.schema}"
  - name: LLM_ENDPOINT
    value: "databricks-claude-sonnet-4"
